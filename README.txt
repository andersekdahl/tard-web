# Tard

Before running anything, make sure you have Node.js, npm, and Leiningen installed.

## Run

Run `make build` to build once, and open `dist/index.html` in your browser to view. Run `make watch` to automatically rebuild when changing files.